# Copy to .env — all real secrets are resolved from Vault at startup
VAULT_ADDR=http://localhost:8200
VAULT_ROLE_ID=fake-role-id
VAULT_SECRET_ID=fake-secret-id
ENVIRONMENT=local
SERVICE_NAME=maintainer-copilot
